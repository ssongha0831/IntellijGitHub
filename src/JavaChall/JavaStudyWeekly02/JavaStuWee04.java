package JavaChall.JavaStudyWeekly02;

import java.util.ArrayList;
import java.util.Scanner;

public class JavaStuWee04 {
    public static void main(String[] args) {
/*
        Scanner scanner = new Scanner(System.in);

        ArrayList<String> phoneNum = new ArrayList<>();
        ArrayList<String> name = new ArrayList<>();
        //ArrayList<String> valuPut = new ArrayList<>();
        String phNum; // 전화번호
        String na; // 이름
        String vaPut; // 명령
        //vaPut = scanner.next();

        System.out.println("연락처 관리 시스템 ( add, remove, list, serch, exit )");
        System.out.print("명령 입력 : ");
        vaPut = scanner.next();
        if (vaPut != "exit") {
            while (true) {
                //vaPut = scanner.next();
                switch (vaPut) {
                    case "add":
                        System.out.print("이름 : ");
                        na = scanner.next();
                        name.add(na);
                        System.out.print("전화번호 : ");
                        phNum = scanner.next();
                        phoneNum.add(phNum);
                        System.out.println("연락처가 추가 되었습니다");

                        break;

                    case "serch":
                        System.out.print("검색할 이름 : ");
                        na = scanner.next();

                        for (int i = 0; i < name.size(); i++) {
                            System.out.println(na + " - " + phoneNum.get(i));
                        } // 선택된 이름이 아닌 전체 리스트가 나옴..ㅠㅠㅠ 해결하기


                    /*
                    for (int i = 0; i < name.size(); i++) {
                        //System.out.println(na + " - " + phoneNum.get(i));
                        if ( na == name.get(i)) {
                            System.out.println("등록되지 않은 이름 입니다.");
                        } else {
                            System.out.println(na + " - " + phoneNum.get(i));
                        }
                        continue;
                    }
                    /*
                    if (name.indexOf(na) == -1) {
                        System.out.println("등록되지 않은 이름 입니다.");
                    } else {
                        System.out.println(na + " - " + phoneNum);
                    }*
                        break;

                    case "list":
                        for (int i = 0; i < name.size(); i++) {
                            System.out.println(name.get(i) + " - " + phoneNum.get(i));
                        }
                        break;

                    case "remove":
                        System.out.print("삭제할 연락처 이름 : ");
                        na = scanner.next();
                        name.remove(na);

                        System.out.println("삭제 완료!");

                    /*
                    if (name.isEmpty() == true) {
                        System.out.println("목록에 없습니다.");
                    } else {
                        for (int i = 0; i < name.size(); i++){
                            na = (String) name.get(i);
                            System.out.println("삭제 완료!");
                        }
                    }

                    *

                        break;
/*
                    case "exit":
                        System.out.println("종료합니다.");
                        break;
*
                    //default:
                }
                System.out.println();
                System.out.println("연락처 관리 시스템 ( add, remove, list, serch, exit )");
                //System.out.println("명령 입력 : ");

            }
            //System.out.println("연락처 관리 시스템 ( add, remove, list, serch, exit )");
            //System.out.println("명령 입력 : ");
        } else if (vaPut == "exit") {
            System.out.println("종료합니다.");
            //break;
        }
        */
    }

}
